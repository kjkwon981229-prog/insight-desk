{
  "name": "insight-desk-push",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Minimal authenticated Web Push delivery Worker for Insight Desk",
  "scripts": {
    "test": "node --test",
    "deploy:dry-run": "wrangler deploy --dry-run"
  },
  "dependencies": {
    "web-push": "3.6.7"
  },
  "devDependencies": {
    "wrangler": "4.120.0"
  }
}
