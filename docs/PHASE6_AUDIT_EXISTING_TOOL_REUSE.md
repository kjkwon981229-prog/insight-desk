# Existing helper reuse

Before adding a new dependency, test whether EvidenceSegmenter, deterministic event rules, Kiwi morphology/offsets, RapidFuzz alias retrieval, and existing benchmark constraints can close the measured gap together. New tools are justified only by incremental evidence.
