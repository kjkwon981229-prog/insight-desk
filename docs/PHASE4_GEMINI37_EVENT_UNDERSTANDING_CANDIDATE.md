# Gemini 3.7 Flash Event Understanding V4 candidate

This document marks the single frozen-contract qualification attempt for `gemini-3.7-flash` under Event Understanding protocol 4.

The provider transport is qualification-only and remains isolated from the production Gemini verification-failover owner. The frozen V4 fixture, prompt, structured-output schema, semantic scopes, gold data, scorer, and 4/4 acceptance threshold are unchanged.

No retry or candidate-specific tuning is authorized after the first real V4 result. Production wiring, migration-blocker removal, fresh-live canary, deploy, Push, and merge remain closed until an active-protocol provider reaches `MINIMUM_COMPATIBILITY_PASS` and the subsequent migration gates pass.
