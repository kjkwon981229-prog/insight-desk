# No-user-work rule

The assistant handles repository changes, tool audits, canaries, CI, failure analysis, and regression work. User action is requested only when an external credential or identity-bound action is technically impossible to perform otherwise; normal semantic operation must never require it.
