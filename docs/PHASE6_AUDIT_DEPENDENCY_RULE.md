# Dependency minimization

Do not add a large ML stack when a smaller deterministic or lexical helper can close the same measured gap. Dependency size, transitive surface, install time, and CI stability are part of the acceptance decision.
