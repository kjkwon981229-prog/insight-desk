# Evidence rule

A helper may propose structure but may not create unsupported text. Every FactDraft field that becomes an EventFact must remain traceable to exact EvidenceSpan text or to a deterministic normalization that preserves the original source expression alongside the normalized value.
